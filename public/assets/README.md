Drop a pre-rendered `hero-video.mp4` here if you want a guaranteed-safe fallback
clip to cut to during the live demo, in case a venue's wifi makes live
generation calls unreliable. Not required — the app runs fully in demo mode
without it.
